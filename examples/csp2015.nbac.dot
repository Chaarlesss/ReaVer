digraph G {
  
  -4 [shape=box,fontsize=10,height=0.01,width=0.01,label="(-4) not anonout_turn and not init and ok"];
  -1 [shape=box,fontsize=10,height=0.01,width=0.01,label="(-1) init"];
  -5 [shape=box,fontsize=10,height=0.01,width=0.01,label="(-5) anonout_turn and not init and ok"];
  -2 [shape=box,fontsize=10,height=0.01,width=0.01,label="(-2) not init and not ok"];
  6 [shape=plaintext,fontsize=10,height=0.01,width=0.01,label="(6) \nnot in1 and not in2 and not proc and -in1_i+proc_i+1>=0 and in2_i-proc_i-1>=0and -in2_i+proc_i+1>=0 and in1_i-in2_i>=0 and in1_i-in2_i-1>=0 ornot in1 and not proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 and in1_i-in2_i>=0 and in1_i-in2_i-1>=0 ornot in1 and not in2 and not proc and -in1_i+proc_i+1>=0and -in2_i+proc_i+1>=0 and in1_i-in2_i>=0 and -in1_i+in2_i>=0 ornot in1 and not in2 and not proc and -in1_i+proc_i+1>=0 and in2_i-proc_i-1>=0and -in2_i+proc_i+1>=0 and -in1_i+in2_i-1>=0 and in1_i-in2_i-1>=0 ornot in1 and not proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 and -in1_i+in2_i-1>=0 and in1_i-in2_i-1>=0 ornot in1 and not in2 and not proc and -in1_i+proc_i+1>=0and -in2_i+proc_i+1>=0 and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 orin1 and not in2 and not proc and -in1_i+proc_i>=0 and in2_i-proc_i-1>=0and -in2_i+proc_i+1>=0 and -in1_i+in2_i-1>=0 and in1_i-in2_i-1>=0 orin1 and not proc and -in1_i+proc_i>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 and -in1_i+in2_i-1>=0 and in1_i-in2_i-1>=0 orin1 and not in2 and not proc and -in1_i+proc_i>=0 and -in2_i+proc_i+1>=0and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 ornot in1 and not in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 and in1_i-in2_i>=0 and in1_i-in2_i-1>=0 ornot in1 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+1>=0and in1_i-in2_i>=0 and in1_i-in2_i-1>=0 ornot in1 and not in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 and in1_i-in2_i>=0 and -in1_i+in2_i>=0 ornot in1 and not in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+1>=0and in1_i-in2_i>=0 and -in1_i+in2_i>=0 ornot in1 and not in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 and -in1_i+in2_i-1>=0 and in1_i-in2_i-1>=0 ornot in1 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+1>=0and -in1_i+in2_i-1>=0 and in1_i-in2_i-1>=0 ornot in1 and not in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 ornot in1 and not in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+1>=0and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 orin1 and not in2 and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 and -in1_i+in2_i-1>=0 and in1_i-in2_i-1>=0 orin1 and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+1>=0and -in1_i+in2_i-1>=0 and in1_i-in2_i-1>=0 orin1 and not in2 and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 orin1 and not in2 and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+1>=0and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 -->\nanon_i'={ anon_i+1 IF anon, anon_i IF not anon }; anonout_turn'=anon; in1_i'={ in1_i+1 IF in1, in1_i IF not in1 }; in2_i'={ in2_i+1 IF in2,in2_i IF not in2 }; init'=false; ok'=true; out_i'={ out_i+1 IF out, out_i IF not out }; proc_i'={ proc_i+1 IF proc,proc_i IF not proc }; "];
  8 [shape=plaintext,fontsize=10,height=0.01,width=0.01,label="(8) \nnot in1 and not in2 and not out and not proc and -in1_i+proc_i+1>=0and in2_i-proc_i-1>=0 and -in2_i+proc_i+1>=0 ornot in1 and not out and not proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 orin1 and not in2 and not out and not proc and -in1_i+proc_i>=0and in2_i-proc_i-1>=0 and -in2_i+proc_i+1>=0 orin1 and not out and not proc and -in1_i+proc_i>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 ornot in1 and not in2 and not out and proc and -in1_i+proc_i+2>=0and -in2_i+proc_i+2>=0 and in2_i-proc_i-2>=0 ornot in1 and not out and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+1>=0 orin1 and not in2 and not out and proc and -in1_i+proc_i+1>=0and -in2_i+proc_i+2>=0 and in2_i-proc_i-2>=0 orin1 and not out and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+1>=0 -->\nanon_i'={ anon_i+1 IF anon, anon_i IF not anon }; anonout_turn'=true; in1_i'={ in1_i+1 IF in1, in1_i IF not in1 }; in2_i'={ in2_i+1 IF in2,in2_i IF not in2 }; init'=false; ok'=true; out_i'=out_i; proc_i'={ proc_i+1 IF proc, proc_i IF not proc }; "];
  7 [shape=plaintext,fontsize=10,height=0.01,width=0.01,label="(7) \nnot in1 and not in2 and out and not proc and -in1_i+proc_i+1>=0and in2_i-proc_i-1>=0 and -in2_i+proc_i+1>=0 ornot in1 and out and not proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 orin1 and not in2 and out and not proc and -in1_i+proc_i>=0and in2_i-proc_i-1>=0 and -in2_i+proc_i+1>=0 orin1 and out and not proc and -in1_i+proc_i>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 ornot in1 and not in2 and out and proc and -in1_i+proc_i+2>=0and -in2_i+proc_i+2>=0 and in2_i-proc_i-2>=0 ornot in1 and out and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+1>=0 orin1 and not in2 and out and proc and -in1_i+proc_i+1>=0and -in2_i+proc_i+2>=0 and in2_i-proc_i-2>=0 orin1 and out and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+1>=0 -->\nanon_i'={ anon_i+1 IF anon, anon_i IF not anon }; anonout_turn'=anon; in1_i'={ in1_i+1 IF in1, in1_i IF not in1 }; in2_i'={ in2_i+1 IF in2,in2_i IF not in2 }; init'=false; ok'=true; out_i'=out_i+1; proc_i'={ proc_i+1 IF proc,proc_i IF not proc }; "];
  12 [shape=plaintext,fontsize=10,height=0.01,width=0.01,label="(12) \nnot in1 and not proc and in1_i-proc_i-2>=0 ornot in1 and not proc and -in1_i+proc_i+1>=0 and in2_i-proc_i-2>=0 ornot in1 and in2 and not proc and -in1_i+proc_i+1>=0 and in2_i-proc_i-1>=0and -in2_i+proc_i+1>=0 orin1 and not proc and in1_i-proc_i-1>=0 orin1 and not proc and -in1_i+proc_i>=0 and in2_i-proc_i-2>=0 orin1 and in2 and not proc and -in1_i+proc_i>=0 and in2_i-proc_i-1>=0and -in2_i+proc_i+1>=0 ornot in1 and proc and in1_i-proc_i-3>=0 ornot in1 and proc and -in1_i+proc_i+2>=0 and in2_i-proc_i-3>=0and in2_i-proc_i-2>=0 ornot in1 and in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 orin1 and proc and in1_i-proc_i-2>=0 orin1 and proc and -in1_i+proc_i+1>=0 and in2_i-proc_i-3>=0and in2_i-proc_i-2>=0 orin1 and in2 and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 -->\nanon_i'={ anon_i+1 IF anon, anon_i IF not anon }; anonout_turn'=not out or anon and out; in1_i'={ in1_i+1 IF in1,in1_i IF not in1 }; in2_i'={ in2_i+1 IF in2, in2_i IF not in2 }; init'=false; ok'=false; out_i'={ out_i+1 IF out,out_i IF not out }; proc_i'={ proc_i+1 IF proc, proc_i IF not proc }; "];
  9 [shape=plaintext,fontsize=10,height=0.01,width=0.01,label="(9) \nnot in1 and in2 and not proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 and in1_i-in2_i>=0 and -in1_i+in2_i>=0 orin1 and not in2 and not proc and -in1_i+proc_i>=0 and in2_i-proc_i-1>=0and -in2_i+proc_i+1>=0 and in1_i-in2_i>=0 orin1 and not proc and -in1_i+proc_i>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 and in1_i-in2_i>=0 ornot in1 and in2 and not proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 orin1 and in2 and not proc and -in1_i+proc_i>=0 and -in2_i+proc_i>=0and -in2_i+proc_i+1>=0 and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 ornot in1 and in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+1>=0and in1_i-in2_i>=0 and -in1_i+in2_i>=0 orin1 and not in2 and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 and in1_i-in2_i>=0 orin1 and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+1>=0 and in1_i-in2_i>=0 ornot in1 and in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+1>=0and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 orin1 and in2 and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+1>=0and -in1_i+in2_i-1>=0 and -in1_i+in2_i>=0 -->\nanon_i'={ anon_i+1 IF anon, anon_i IF not anon }; anonout_turn'=anon; in1_i'={ in1_i+1 IF in1, in1_i IF not in1 }; in2_i'={ in2_i+1 IF in2,in2_i IF not in2 }; init'=false; ok'=true; out_i'={ out_i+1 IF out, out_i IF not out }; proc_i'={ proc_i+1 IF proc,proc_i IF not proc }; "];
  11 [shape=plaintext,fontsize=10,height=0.01,width=0.01,label="(11) \nnot in1 and not proc and in1_i-proc_i-2>=0 ornot in1 and not proc and -in1_i+proc_i+1>=0 and in2_i-proc_i-2>=0 ornot in1 and in2 and not proc and -in1_i+proc_i+1>=0 and in2_i-proc_i-1>=0and -in2_i+proc_i+1>=0 orin1 and not proc and in1_i-proc_i-1>=0 orin1 and not proc and -in1_i+proc_i>=0 and in2_i-proc_i-2>=0 orin1 and in2 and not proc and -in1_i+proc_i>=0 and in2_i-proc_i-1>=0and -in2_i+proc_i+1>=0 ornot in1 and proc and in1_i-proc_i-3>=0 ornot in1 and proc and -in1_i+proc_i+2>=0 and in2_i-proc_i-3>=0and in2_i-proc_i-2>=0 ornot in1 and in2 and proc and -in1_i+proc_i+2>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 orin1 and proc and in1_i-proc_i-2>=0 orin1 and proc and -in1_i+proc_i+1>=0 and in2_i-proc_i-3>=0and in2_i-proc_i-2>=0 orin1 and in2 and proc and -in1_i+proc_i+1>=0 and -in2_i+proc_i+2>=0and in2_i-proc_i-2>=0 -->\nanon_i'={ anon_i+1 IF anon, anon_i IF not anon }; anonout_turn'=anon; in1_i'={ in1_i+1 IF in1, in1_i IF not in1 }; in2_i'={ in2_i+1 IF in2,in2_i IF not in2 }; init'=false; ok'=false; out_i'={ out_i+1 IF out, out_i IF not out }; proc_i'={ proc_i+1 IF proc,proc_i IF not proc }; "];
  10 [shape=plaintext,fontsize=10,height=0.01,width=0.01,label="(10) \nanon_i'=0; anonout_turn'=false; in1_i'=0; in2_i'=0; init'=false; ok'=true; out_i'=0; proc_i'=0; "];
  -4 -> 6;
  6 -> -4;
  -5 -> 8;
  8 -> -5;
  -5 -> 7;
  7 -> -4;
  -5 -> 12;
  12 -> -2;
  -4 -> 9;
  9 -> -5;
  -4 -> 11;
  11 -> -2;
  -1 -> 10;
  10 -> -4;
  
}
